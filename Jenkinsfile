#!/usr/bin/env groovy
node {
  stage('Build') {
    echo 'Building...'
    echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
  }  
  stage('Test') {
    echo 'Testing...'
  }
  stage('Deploy') {
    echo 'Deploying...'
  }
}
